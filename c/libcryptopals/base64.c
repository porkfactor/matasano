#include <pm/base64.h>

ssize_t pm_base64_encode(
        char *buffer,
        size_t szBuffer,
        void const *data,
        size_t szData)
{
    ssize_t rv = -1;

    return rv;
}

ssize_t pm_base64_decode(
        void *buffer,
        size_t szBuffer,
        char const *data,
        size_t szData)
{
    ssize_t rv = -1;

    return rv;
}
